in project's folder headline, edit its locations' text to 'cmd', hit enter and type:
'pyinstaller --onefile -w main.py'
*the '-w' is optional, it hides the console and show only the GUI, if there's one
*use nsis to build installers from zip of folders

python -m pip install --upgrade pip

pip --version